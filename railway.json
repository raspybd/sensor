   {
     "build": {
       "builder": "nixpacks",
       "nixpacks": {
         "phases": {
           "install": "npm install -g http-server"
         },
         "start": "http-server -p $PORT"
       }
     }
   }
